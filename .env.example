# Copy to .env and fill in values (only needed for development overrides)
# Runtime config is managed via Settings UI -> userData/settings.json

# Dev: override renderer URL (set automatically by electron-vite)
# ELECTRON_RENDERER_URL=http://localhost:5173
